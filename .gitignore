# 依赖
node_modules

# 构建产物
dist
build

# 日志
*.log

# 开发环境缓存
.DS_Store
Thumbs.db

# Vercel / Netlify 本地缓存
.vercel
.netlify

# 注：vercel.json 曾因 source 写成 "/((?!api/).*)" 这种负向断言被 Vercel 判为
# "Invalid vercel.json"，当时靠把它加进 .gitignore 绕过去了——但那样 Vercel 侧
# 等于没有任何 rewrite 配置，深路径全部 404。现已改回合法写法 "/(.*)" 并重新纳入版本控制。

